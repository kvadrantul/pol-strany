# 🔍 Проверка деплоя приложения

## ✅ Что нужно проверить:

### 1. Проверка через Vercel Dashboard (самый надежный способ)

1. Откройте: https://vercel.com/dashboard
2. Найдите проект **pol-strany**
3. Перейдите в раздел **Deployments**
4. Проверьте последний деплой:

   - ✅ **Ready** (зеленый) - деплой успешен
   - 🟡 **Building** (желтый) - деплой в процессе
   - ❌ **Error** (красный) - ошибка деплоя

5. Если деплой успешен, нажмите на него и найдите **URL** (обычно в верхней части страницы)

### 2. Проверка URL проекта

Ваш проект должен быть доступен по одному из URL:

- `https://pol-strany.vercel.app` (основной домен)
- `https://pol-strany-[random].vercel.app` (с уникальным ID)
- `https://pol-strany-git-main-[team].vercel.app` (для ветки main)

**Где найти точный URL:**
- В Vercel Dashboard → ваш проект → Overview
- В Vercel Dashboard → Deployments → последний деплой → URL

### 3. Проверка работы приложения

После того как нашли правильный URL, проверьте:

#### ✅ Главная страница
```
https://[your-url].vercel.app/
```
Должна открыться HTML страница приложения

#### ✅ API endpoint
```
https://[your-url].vercel.app/api/tariffs
```
Должен вернуться JSON с тарифами:
```json
{
  "econom": {...},
  "comfort": {...},
  ...
}
```

#### ✅ Статические файлы
```
https://[your-url].vercel.app/app.js
https://[your-url].vercel.app/styles.css
https://[your-url].vercel.app/index.html
https://[your-url].vercel.app/images/logo.svg
```

Все должны вернуть соответствующие файлы (не 404).

### 4. Если получаете 404:

#### Возможные причины:

1. **Деплой еще не завершился**
   - Подождите 1-2 минуты
   - Проверьте статус в Vercel Dashboard

2. **Неправильный URL**
   - Используйте URL из Vercel Dashboard
   - Не используйте примеры из документации

3. **Ошибка деплоя**
   - Проверьте логи в Vercel Dashboard
   - Убедитесь, что все переменные окружения добавлены

4. **Проблема с конфигурацией**
   - Проверьте `vercel.json`
   - Убедитесь, что структура проекта правильная

### 5. Проверка через терминал:

```bash
# Замените [your-url] на реальный URL из Vercel
URL="https://[your-url].vercel.app"

# Проверка главной страницы
curl -I "$URL/"

# Проверка API
curl "$URL/api/tariffs"

# Проверка статики
curl -I "$URL/app.js"
curl -I "$URL/styles.css"
```

## 🔧 Быстрое решение проблем:

### Проблема: Деплой не запускается
**Решение:** Проверьте подключение репозитория в Settings → Git

### Проблема: Деплой падает с ошибкой
**Решение:** 
1. Проверьте логи в Vercel Dashboard
2. Убедитесь, что все переменные окружения добавлены
3. Проверьте `vercel.json` на ошибки

### Проблема: 404 на всех страницах
**Решение:**
1. Проверьте `outputDirectory` в `vercel.json` (должно быть `.`)
2. Убедитесь, что файлы в корне проекта
3. Передеплойте проект

### Проблема: API возвращает ошибку
**Решение:**
1. Проверьте переменные `DATABASE_URL` и `TURSO_AUTH_TOKEN`
2. Проверьте логи функций в Vercel Dashboard
3. Убедитесь, что база данных доступна

## 📊 Ссылки для проверки:

- **Vercel Dashboard:** https://vercel.com/dashboard
- **Project Settings:** https://vercel.com/team_igUeUyoVL5L5eyqwTPs4kqVy/pol-strany/settings
- **Environment Variables:** https://vercel.com/team_igUeUyoVL5L5eyqwTPs4kqVy/pol-strany/settings/environment-variables
- **Deployments:** https://vercel.com/team_igUeUyoVL5L5eyqwTPs4kqVy/pol-strany/deployments

## ✅ Чек-лист проверки:

- [ ] Проект виден в Vercel Dashboard
- [ ] Последний деплой имеет статус "Ready"
- [ ] Найден правильный URL проекта
- [ ] Главная страница открывается (не 404)
- [ ] API endpoint `/api/tariffs` возвращает JSON
- [ ] Статические файлы загружаются
- [ ] Нет ошибок в логах деплоя

## 🆘 Если ничего не помогает:

1. Проверьте логи деплоя в Vercel Dashboard
2. Убедитесь, что репозиторий правильно подключен
3. Передеплойте проект вручную:
   ```bash
   vercel --prod
   ```
4. Обратитесь к документации: https://vercel.com/docs

