# 🔥 КРИТИЧЕСКАЯ ПРОБЛЕМА: Статика не отдается

## Проблема:
404: NOT_FOUND для всех статических файлов (index.html, app.js, styles.css)

## Корневая причина:
Когда используется Go handler через `builds`, Vercel создает catch-all handler, который перехватывает ВСЕ запросы, включая статику. Handler возвращает 404 для всех не-API запросов.

## Решение:

### Вариант 1: Использовать только rewrites (без builds)

Vercel автоматически обнаружит Go файл и использует правильный runtime. Убрать `builds` из vercel.json.

### Вариант 2: Изменить Handler чтобы он не перехватывал статику

Handler должен возвращать пустой ответ или пропускать статику, чтобы Vercel мог отдать её автоматически.

### Вариант 3: Использовать public директорию

Переместить статику в `public/` и настроить Vercel на обслуживание из этой директории.

## Текущая конфигурация:

```json
{
  "version": 2,
  "outputDirectory": ".",
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/index.go"
    }
  ]
}
```

БЕЗ `builds` - Vercel автоматически обнаружит Go runtime.

## Проверка:

После деплоя проверьте:
- `https://your-url.vercel.app/` - должна открыться index.html
- `https://your-url.vercel.app/app.js` - должен отдаться файл
- `https://your-url.vercel.app/api/tariffs` - должен работать API

