# 🚀 Деплой Go бэкенда на Vercel

## ✅ Что готово:

- ✅ Go бэкенд создан и компилируется
- ✅ Все API endpoints реализованы
- ✅ Подключение к Turso настроено
- ✅ `vercel.json` настроен для Go

## 📋 Деплой:

### 1. Убедитесь что переменные окружения добавлены в Vercel:

В Vercel Dashboard → Settings → Environment Variables:

```
DATABASE_URL = libsql://pol-strany-hun7eee.aws-eu-west-1.turso.io
TURSO_AUTH_TOKEN = eyJhbGciOiJFZERTQSIsInR5cCI6IkpXVCJ9...
TELEGRAM_BOT_TOKEN = 8510455686:AAEvlK1P3_yt6btesENX_-d2OMDn5H6T1AQ
NODE_ENV = production
```

### 2. Деплой:

```bash
cd pol-strany
vercel --prod
```

Vercel автоматически:
- Определит Go проект
- Установит зависимости (`go mod download`)
- Соберет проект
- Задеплоит

### 3. Проверка:

После деплоя проверьте:

```
https://your-project.vercel.app/api/tariffs
```

Должен вернуться JSON с тарифами.

## 🔍 Структура проекта:

```
pol-strany/
├── backend/
│   ├── main.go          # Точка входа
│   ├── db.go            # Работа с БД
│   ├── handlers.go      # HTTP handlers
│   ├── go.mod           # Go модуль
│   └── vercel.json      # Конфигурация Vercel
├── public/              # Frontend (без изменений)
└── vercel.json          # Главная конфигурация
```

## ⚠️ Важно:

- Vercel автоматически определит Go проект по `go.mod`
- Все статические файлы из `public/` будут обслуживаться отдельно
- API routes (`/api/*`) будут обрабатываться Go бэкендом

## 🐛 Если что-то не работает:

1. Проверьте логи в Vercel Dashboard
2. Убедитесь что все переменные окружения добавлены
3. Проверьте что `vercel.json` указывает на `backend/main.go`

