# 🚀 Быстрый старт - Деплой на Vercel

## ✅ Что уже готово:

- ✅ Код адаптирован для Turso Database
- ✅ Логотип добавлен в приложение  
- ✅ Токен бота: `8510455686:AAEvlK1P3_yt6btesENX_-d2OMDn5H6T1AQ`
- ✅ Токен Vercel: `AMfApuKRnYM2Kwd4UC1GxRVb`
- ✅ Структура проекта готова для деплоя

## 📋 Шаги для деплоя:

### 1. Получить данные Turso Database

1. Зайдите на https://turso.tech
2. Войдите или создайте аккаунт
3. Создайте новую базу данных
4. Скопируйте:
   - **Database URL** (формат: `libsql://your-db-name.turso.io`)
   - **Auth Token** (Settings → Auth Tokens)

### 2. Деплой на Vercel

```bash
# Установите Vercel CLI (если еще не установлен)
npm i -g vercel

# Войдите в Vercel
vercel login

# Перейдите в директорию проекта
cd pol-strany

# Деплой
vercel
```

Следуйте инструкциям в терминале.

### 3. Добавить переменные окружения

После деплоя откройте Vercel Dashboard:

1. Выберите проект
2. **Settings** → **Environment Variables**
3. Добавьте:

```
TELEGRAM_BOT_TOKEN = 8510455686:AAEvlK1P3_yt6btesENX_-d2OMDn5H6T1AQ
DATABASE_URL = libsql://your-database-name.turso.io
TURSO_AUTH_TOKEN = your-turso-auth-token
APP_URL = https://your-project.vercel.app
NODE_ENV = production
```

**⚠️ Важно:** Замените `your-database-name.turso.io` и `your-turso-auth-token` на реальные значения!

### 4. Передеплой

```bash
vercel --prod
```

### 5. Настроить Telegram Mini App

1. Откройте [@BotFather](https://t.me/BotFather)
2. `/newapp`
3. Выберите вашего бота
4. Укажите:
   - **Title:** Пол Страны
   - **Web App URL:** `https://your-project.vercel.app`
   - Остальное по желанию

### 6. Готово! 🎉

Откройте бота в Telegram → `/start` → "Открыть приложение"

## 📁 Структура проекта:

```
pol-strany/
├── api/
│   ├── index.js          # Entry point для Vercel
│   └── routes.js         # API роуты
├── public/               # Frontend
│   ├── index.html
│   ├── styles.css
│   ├── app.js
│   └── images/
│       └── logo.svg      # Логотип
├── db-turso.js           # Адаптер для Turso
├── db.js                 # SQLite (для локальной разработки)
├── bot.js                # Telegram бот
├── vercel.json           # Конфигурация Vercel
└── package.json
```

## 🔧 Локальная разработка:

```bash
# Установить зависимости
npm install

# Инициализировать БД (SQLite)
npm run init-db

# Запустить сервер
npm start

# В другом терминале - запустить бота
npm run start:bot
```

## 📝 Что нужно предоставить:

1. ✅ Токен бота - уже есть
2. ✅ Токен Vercel - уже есть  
3. ⏳ **Turso Database URL** - нужно получить из Turso Dashboard
4. ⏳ **Turso Auth Token** - нужно получить из Turso Dashboard

## 🐛 Проблемы?

См. подробную инструкцию в `DEPLOY_NOW.md`

