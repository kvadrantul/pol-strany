# Автоматический деплой через Vercel

Проект настроен для автоматического деплоя при каждом коммите в ветку `main`.

## 🚀 Настройка автоматического деплоя

### Шаг 1: Подключение репозитория к Vercel

1. Перейдите на https://vercel.com
2. Войдите в свой аккаунт (или создайте новый)
3. Нажмите **"Add New..."** → **"Project"**
4. Найдите репозиторий **`kvadrantul/pol-strany`** в списке
5. Нажмите **"Import"**

### Шаг 2: Настройка проекта

Vercel автоматически определит конфигурацию из `vercel.json`:

- **Framework Preset:** Other
- **Root Directory:** `./` (корень проекта)
- **Build Command:** (пусто, так как статика уже готова)
- **Output Directory:** `.` (корень проекта)

### Шаг 3: Добавление переменных окружения

В настройках проекта добавьте следующие переменные:

#### Обязательные:
```
DATABASE_URL=libsql://pol-strany-hun7eee.aws-eu-west-1.turso.io
TURSO_AUTH_TOKEN=your_turso_auth_token_here
TELEGRAM_BOT_TOKEN=your_telegram_bot_token_here
NODE_ENV=production
```

#### Как добавить:
1. В настройках проекта перейдите в **Settings** → **Environment Variables**
2. Добавьте каждую переменную отдельно
3. Выберите окружения: **Production**, **Preview**, **Development**
4. Нажмите **Save**

### Шаг 4: Первый деплой

1. Нажмите **"Deploy"**
2. Дождитесь завершения деплоя (обычно 1-2 минуты)
3. Получите URL вида: `https://pol-strany-xxx.vercel.app`

## ✅ Автоматический деплой настроен!

После подключения репозитория:
- ✅ Каждый коммит в ветку `main` автоматически запустит деплой
- ✅ Pull Request'ы будут создавать Preview деплои
- ✅ Все изменения будут автоматически доступны на production URL

## 🔄 Как это работает

1. Вы делаете изменения в коде
2. Коммитите и пушите в GitHub:
   ```bash
   git add .
   git commit -m "Описание изменений"
   git push
   ```
3. Vercel автоматически обнаруживает новый коммит
4. Запускается процесс деплоя (можно отслеживать в Dashboard)
5. Через 1-2 минуты изменения доступны на production URL

## 📊 Мониторинг деплоев

- **Vercel Dashboard:** https://vercel.com/dashboard
  - Видите все деплои и их статусы
  - Просматриваете логи сборки
  - Проверяете ошибки

- **GitHub Actions** (опционально):
  Можно также настроить GitHub Actions для дополнительных проверок

## 🔍 Проверка работы

После первого деплоя проверьте:

1. **Главная страница:**
   ```
   https://your-project.vercel.app/
   ```

2. **API endpoint:**
   ```
   https://your-project.vercel.app/api/tariffs
   ```

3. **Статические файлы:**
   ```
   https://your-project.vercel.app/app.js
   https://your-project.vercel.app/styles.css
   ```

## 🛠️ Ручной деплой (если нужно)

Если нужно задеплоить вручную:

```bash
cd pol-strany
vercel --prod
```

## 📝 Структура проекта

```
pol-strany/
├── index.html          # Главная страница (в корне)
├── app.js              # JavaScript (в корне)
├── styles.css          # CSS (в корне)
├── images/             # Изображения (в корне)
├── api/
│   └── index.go        # Go handler для Vercel
├── backend/
│   └── main.go         # Backend для локальной разработки
└── vercel.json         # Конфигурация Vercel
```

## ⚠️ Важные заметки

1. **База данных:** Убедитесь, что `DATABASE_URL` и `TURSO_AUTH_TOKEN` правильно настроены
2. **Telegram Bot:** Обновите URL Mini App в BotFather на ваш Vercel URL
3. **Переменные окружения:** Все секреты должны быть в Environment Variables, не в коде!

## 🆘 Проблемы?

Если деплой не работает:

1. Проверьте логи в Vercel Dashboard
2. Убедитесь, что все переменные окружения добавлены
3. Проверьте, что `vercel.json` правильно настроен
4. Убедитесь, что репозиторий подключен к Vercel

